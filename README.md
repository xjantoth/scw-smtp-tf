# Usage

```bash
terraform init
terraform validate
terraform fmt -recursive
terraform plan -var-file=terraform.mail.tfvars
terraform apply -var-file=terraform.mail.tfvars
terraform destroy -var-file=terraform.mail.tfvars
```